services:
  postgres:
    image: postgres:15-alpine
    container_name: agent-postgres
    environment:
      POSTGRES_DB: agent_system
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent -d agent_system"]
      interval: 5s
      timeout: 5s
      retries: 5
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  api:
    build: .
    container_name: agent-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg://agent:agent_password@postgres:5432/agent_system
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      WORKSPACE_DIR: /app/workspace
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      NEWSAPI_KEY: ${NEWSAPI_KEY:-}
      COSTS_DIR: /app/costs
      SHARED_WORKSPACE: /app/workspace/shared
      ENABLE_SHELL: "false"
      ENABLE_PYTHON_EXECUTOR: "true"
      SANDBOX_DIR: /app/sandbox
      TOKENIZERS_PARALLELISM: "false"
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:          
        condition: service_started
    volumes:
      - ./workspace:/app/workspace      
      - ./sandbox:/app/sandbox           
      - ./traces:/app/traces      
      - ./costs:/app/costs
      - ./evaluation_results:/app/evaluation_results   # ← add this       
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 

volumes:
  postgres_data:
  qdrant_data: